# deprecated

`assistant/` was prompt-only policy. Canonical contracts are in `docs/`. Executable modules are in `src/board_game_companion/companion/`. Historical files remain for git history; do not treat them as the runtime.
